let registerRecipeTypes = (/** @type {Registry.Recipe_Type} */ event) => {
  // only use this when registering non-multiblock machine recipe types. Multiblock recipe types should be co-located
  // with their machine definition in the multiblock subfolder.
}

let registerMachines = (/** @type {Registry.Machine} */ event) => {
  // only use this when registering non-multiblock machines. Multiblock definitions should be placed inside of the
  // multiblocks folder.
}
